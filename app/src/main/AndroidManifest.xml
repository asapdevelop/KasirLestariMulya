<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asap20.com.lestarimulya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PenjualanActivity"
            android:label="@string/title_activity_penjualan"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".TarikTabunganActivity" />
        <activity android:name=".DetailPelanggan" />
        <activity android:name=".BayarHutangActivity" />
        <activity android:name=".DataPelangganActivity" />
        <activity android:name=".Transaksi" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>